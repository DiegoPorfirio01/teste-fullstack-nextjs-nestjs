# Provisioning de dashboards - carregados automaticamente
apiVersion: 1

providers:
  - name: "teste-api"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards-json
